# Spit dashboard — Mosquitto WebSocket listener.
# Drops in at /etc/mosquitto/conf.d/websockets.conf.
# Keep the default MQTT listener on 1883 untouched (Node-Red + Python script use it).

listener 9001
protocol websockets
allow_anonymous true
